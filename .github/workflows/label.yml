name: "Pull Request Labeler"
on:
- pull_request

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v2
      with:
        repo-token: "${{ SECRETS_GITHUB_TOKEN }}"
        configuration-path: .github/labeler.yml